{
  "name": "soccer-field-booking-management",
  "version": "1.0.0",
  "description": "Full-stack soccer field booking and match coordination app.",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client/web\" --names \"api,web\" --prefix-colors \"green,blue\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "client/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
